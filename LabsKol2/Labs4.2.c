#include <stdio.h>

int main() {

    int m, n;
    scanf("%d%d", &m, &n);

    int arrA[100], arrB[100], arrC[200];

    for (int i = 0; i < m; ++i) {
        scanf("%d", &arrA[i]);
    }
    for (int i = 0; i < n; ++i) {
        scanf("%d", &arrB[i]);
    }


    
    return 0;
}